services:
  - type: web
    name: codecompass-backend
    env: python
    pythonVersion: "3.12.0"
    buildCommand: "bash build.sh"
    startCommand: "/opt/render/project/src/.venv/bin/gunicorn config.asgi:application -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT --workers 1"
